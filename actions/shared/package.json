{
  "name": "@aba/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared helpers for the enterprise pipeline actions (YAML load, JSON Schema validation).",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "yaml": "^2.6.1"
  }
}

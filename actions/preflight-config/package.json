{
  "name": "@pipeline/preflight-config-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action: merge per-app CI config with central pipeline policy + defaults into typed outputs.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@pipeline/shared": "*"
  }
}
